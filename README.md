# amlboot-release

Release binaries of **amlboot** — a command-line tool for flashing Amlogic SoCs
over USB.

Each release provides raw per-target executables (Linux `x86_64` / `i686` /
`aarch64`, static musl) and a `SHA256SUMS`. Downloads are not marked executable;
run `chmod +x` before use.
